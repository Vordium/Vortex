import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // You may have a CSS file to style your app
import App from './App'; // Import your main React component (e.g., App.js)

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
